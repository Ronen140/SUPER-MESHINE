/**
 * @super-meshine/core — domain types & business logic shared between
 * `packages/api` and `packages/agents`.
 *
 * Round 7a is intentionally empty (placeholder) so the ADR-004 monorepo
 * layout is honest. First real domain logic arrives in 7b.
 */
export {};
