/**
 * Schema barrel.
 *
 * Round 7a is intentionally empty — real tables (tenants, users, items, ...)
 * arrive in Round 7b once the first vertical context is selected per ADR-001.
 *
 * Adding a table later: create `./src/schema/<entity>.ts` and re-export here.
 */
export {};
